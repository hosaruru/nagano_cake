
<div class="col-md-8 offset-md-1">
<h2>商品詳細</h2>
<table class="table">

 <thead>
 <tr>
 <th></th>
<th>商品名</th>
<th>商品説明</th>
<th>ジャンル</th>
<th>税込価格（税抜価格）</th>
<th>販売ステータス</th>
</tr>
 </thead>
<tbody>
<tr>
<td>
   <% if @item.image.attached? %>
    <%= image_tag @item.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>
</td>
<td><%= @item.name %></td>
<td><%= @item.introduction %></td>
<td><%= @item.genre.name %></td>
<td><%= @item.price %>円</td>
<td><% if  @item.is_active == true %>
 <p> 販売中</p>
<% else %>
 <p>売り切れ</p>
<% end %></td>

<tr>
</tbody>
</table>
<p><%= link_to "編集する", admin_edit_item_path(@item.id) %></p> 
</div>

